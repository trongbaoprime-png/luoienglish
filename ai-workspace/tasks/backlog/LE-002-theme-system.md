# Task Contract: LE-002 — Dual-Theme System Implementation

- **Task ID**: `LE-002`
- **Goal**: Implement dynamic runtime theme switching between `cozy` (Cozy Lười) and `explorer` (Explorer Lười) with complete token styling and preview widget.
- **Risk Level**: `R1 Feature`
- **Acceptance Criteria**:
  1. `ThemeProvider` context enables switching between `cozy` and `explorer`.
  2. Tailwind CSS variables dynamic switching.
  3. Mascot skin changes based on theme preference.
  4. Business logic completely untouched by theme changes.
